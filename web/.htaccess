RewriteEngine On

# Redirect /invite/CODE to the PHP script
RewriteRule ^invite/([A-Za-z0-9]+)/?$ invite/index.php?code=$1 [L,QSA]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

